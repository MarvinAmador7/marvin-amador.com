# Animation plans

Generated by `improve-animations` against commit `bc57d6e`. Each plan is
self-contained — an executor with zero context can run it. Plans only modify the
files they name; they never touch anything outside `plans/`.

## Plans

| # | Title | Severity | Category | Status |
|---|-------|----------|----------|--------|
| [001](001-projectcard-hover-no-layout-animation.md) | Stop animating `padding` on ProjectCard hover | MEDIUM | Performance | DONE |
| [002](002-siteheader-logo-transform-easing.md) | Give the header logo's rotate/scale a stronger easing | LOW–MED | Easing | DONE |
| [003](003-tokenize-hover-easing.md) | Tokenize the duplicated hover easing | LOW | Cohesion/tokens | DONE |

## Recommended execution order

1. **003** — introduces the `--ease-hover` token and swaps the pure color-hover
   literals. Behavior-preserving; safest to land first.
2. **002** — rewrites the header logo box; **depends on the `--ease-hover` token
   created by 003**. Run after 003.
3. **001** — independent of both; can run any time.

## Dependencies

- **002 → 003**: plan 002 references `var(--ease-hover)`, which plan 003 creates.
  If 002 is run first it will STOP at step 1 by design. Run 003 first.
- **001** has no dependencies and shares no files with 002/003.
- **File partition (no overlaps)**: 001 touches only `components/ProjectCard.tsx`;
  002 touches only `components/SiteHeader.tsx` **line 13**; 003 touches
  `app/globals.css` + several files including `components/SiteHeader.tsx` **line
  29** (never line 13). No two plans edit the same line.

## Notes

- These three were selected from a larger audit. Not turned into plans (optional
  polish): reveal entrance duration (640ms, `globals.css:174`), `transition-all`
  on the unused `components/ui/button.tsx`, hero-line entrance stagger, and route
  transitions. Ask `improve-animations` to plan any of these if wanted.
- The codebase's motion is already high-craft (strong ease-out token,
  blur-masked reveals, press feedback, full reduced-motion coverage). These
  plans are refinements, not rescues.
